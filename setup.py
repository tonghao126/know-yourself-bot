from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Exploration project for fine tuning LLM. Idea is to create a bot to ask existential questions for me to better understand myself (Socratic Method). We'll see how far we can push the envelop :P',
    author='Hao Tong',
    license='',
)
